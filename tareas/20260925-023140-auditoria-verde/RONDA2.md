# Auditoría, ronda 2

Sólo se llama hallazgo al comportamiento reproducido con ejecución. Las reproducciones usan
proyectos temporales o ejemplos del repositorio. La prueba nueva crea su propio proyecto mínimo.

| Sospecha | Comando ejecutado y resultado | Dictamen |
| --- | --- | --- |
| `tools/mcp.py:1663`: `oracle_juzgar` omite medidas propias no aplicadas al calcular `ok` | `python3 -m unittest tests.test_auditoria_ronda2 -v`: antes del arreglo, `test_mcp_no_aprueba_si_una_medida_propia_quedo_sin_aplicar` falló porque MCP devolvió `ok=True`; la CLI devolvió código 1 y `ok=False` con la misma evidencia. Después, el test pasa. | **Hallazgo corregido.** Proyecto mínimo: dos medidas propias, `demo.uno` lee `item`, `demo.dos` lee `otro`; evidencia sólo `{"item":[{"id":1}]}`. MCP evaluaba una y aprobaba la corrida pese a listar `demo.dos` en `no_aplicadas`. `Informe.ok_completo` reúne el criterio de juicio completo, usado por CLI y MCP; la CLI conserva `--parcial`. |
| `tools/aceptacion.py:138-157`, `nucleo/marco.py:282`: un caso de defecto con `SIN EVIDENCIA` satisface la polaridad roja sin valor medido | Proyecto temporal con `catalogo_base:false`, una medida `requiere item` y caso `falso_verde` con `{"item":[]}`: `aceptacion._ejecutar` y `cli.cmd_test(...,["--rapido"])` devolvieron 0, y la salida dice `ROJO ... SIN EVIDENCIA` / `VEREDICTO: VERDE`. Reproducción versionada: `python3 tools/cli.py test --rapido --proyecto ejemplo/batalla-naval` muestra lo mismo en los casos 029 y 030. | **Decisión de diseño, sin arreglo.** Intenté exigir medición y el test rojo reprodujo el riesgo, pero la suite mostró que los casos 029/030 de batalla naval y 006 de primer valor fijan deliberadamente el resultado de `requiere` con ausencia. Volví al comportamiento existente. Recomendación: declarar en el caso la salida esperada `sin_evidencia` como estado distinto del rojo medido, para que la aceptación pueda distinguir ambos sin romper estos casos. |
| `tools/cli.py:855-876`: proyecto vacío devuelve 0 | `python3 -m unittest tests.test_cli tests.test_cli_integracion -q` (incluido en la suite completa): proyecto recién iniciado informa `VEREDICTO: SIN MEDICIÓN`, sin afirmar VERDE. | Descartado como falso verde; es compatibilidad de inicialización y la salida explicita ausencia de medición. |
| `tools/juzgar.py:308`: `--parcial` permite relaciones faltantes | `python3 -m unittest tests.test_juzgar_cota_y_ausentes.NoAplicadasTests.test_parcial_permite_omitir_medidas_propias -v` pasó. | Decisión explícita del usuario de la CLI. Sin `--parcial`, `ok_completo` es falso; con él, la salida sigue enumerando `no_aplicadas`. |
| `tools/mutar.py:85-87` y `nucleo/mutacion.py:437-443`: salida de hechos o caso no reproducido puede saltearse | Proyecto temporal con una medida `requiere item` y caso rojo `item: []`: `mutar._ejecutar(proy, [])` dio código 1, 9 mutantes sobrevivientes. `python3 -m unittest tests.test_mutacion -q` pasó. | No reprodujo verde. Queda una pregunta de diseño sobre si un `SIN EVIDENCIA` debe contar detecciones de mutantes, pero esta entrada no aprobó la ronda. `--hechos` es modo sensor, no juicio. |
| `tools/diferencial.py:89-94`: fixture vencido usa `continue` | `python3 -m unittest tests.test_diferencial_informe.InformeTests.test_un_fixture_vencido_no_se_compara_y_hace_fallar -v` pasó: código 1 y diagnóstico de procedencia vencida. | Descartado. El `continue` evita comparar datos vencidos, pero acumula una falla que impide verde. |
| `tools/corpus.py:248-250`: caso ilegible usa `continue` | `python3 -m unittest tests.test_corpus_cli tests.test_fixtures -q` pasó; `corpus.verificar` acumula la falla antes de saltar ese caso. | Descartado: el salto no borra el error del corpus. |
| `tools/aceptacion.py:291-308`: `--hechos` retorna 0 aunque existan fallas | `python3 -m unittest tests.test_observar -q` pasó; el modo emite los hechos y anuncia cuántos problemas hubo. | Decisión de interfaz: código 0 significa que se pudieron leer y volcar los hechos para observación, no aceptación verde. Recomendación: conservar visible esa distinción en consumidores del modo sensor. |
| `ejemplo/seguimiento-tareas/cierre_medidas.py:18-82`: una medida no aplicada o un error de subprocess podría cerrar una tarea | `python3 -m unittest tests.test_cierre_medidas_integracion -q` pasó; prueba real de medida no evaluada, inexistente, sombra y salida fallida. | Descartado: la política no recibe un hecho `aceptacion_medida` para un id no evaluado y falla el cierre; los errores cortan antes de publicar evidencia. |
| MCP, escalares no confiadas, ids ausentes y desafío sin mutantes | `python3 -m unittest tests.test_mcp_027_revision.BordesDeLaMutacionTests.test_juzgar_sin_autorizacion_no_ejecuta_escalares tests.test_mcp_027_revision.JuzgarTests.test_sin_medidas_aplicables_no_es_un_verde tests.test_mcp.DesafiarLosCaminosQueNadieRecorreTests.test_sin_mutantes_no_es_todos_detectados -v` pasó. | Descartados: error o `ok=False`; el desafío informa `sin_mutantes` en lugar de «todos detectados». |

La discrepancia MCP/CLI era el único falso verde nuevo confirmado que admitía arreglo compatible
con las decisiones existentes. Los tests MCP que esperaban un `ok` parcial por omisión se ajustaron
al mismo criterio que ya aplicaba `oracle juzgar` sin `--parcial`.
