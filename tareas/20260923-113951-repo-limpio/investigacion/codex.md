Leí completos [TAREA.md](../TAREA.md) e [INVENTARIO.md](../INVENTARIO.md), incluida la revisión final, y contrasté las rutas con el árbol. **Recomiendo separar la documentación vigente del producto de la memoria de trabajo**: `docs/` para la primera, `vault-kb/` para la segunda.

[pyproject.toml](../../../pyproject.toml) declara paquetes Python y datos internos; no declara estas decisiones, guías, clientes ni la futura wiki como contenido del wheel. No hay `MANIFEST.in`. El README sí se usa como metadato del paquete, pero un enlace suyo a GitHub no convierte el archivo enlazado en parte de la instalación. La separación coincide con ejemplos verificables: [Flask organiza su tutorial y diseño en `docs/`](https://github.com/pallets/flask/blob/main/docs/index.rst); [Ruff documenta la integración con editores](https://github.com/astral-sh/ruff/blob/main/docs/editors/setup.md) y mantiene [su extensión de VS Code en otro repositorio](https://github.com/astral-sh/ruff-vscode).

| Grupo | Recomendación y motivo | Mejor alternativa | Actualizaciones necesarias |
|---|---|---|---|
| **12 `DECISION-*`** | `docs/decisiones/`, con índice por ID, título y estado. Explican decisiones del producto a quien evalúa Oracle o contribuye; `vault-kb/` quedaría para antecedentes. El inventario omite `DECISION-012`, que sí existe. | Dejarlas en la raíz: conserva los enlaces actuales, pero mantiene la mezcla que originó la tarea. | Cambiar los 11 enlaces de [README.md](../../../README.md) y agregar 012; ajustar [docs/README.md](../../../docs/README.md), las tres rutas rígidas de [tools/estudio.py](../../../tools/estudio.py) y enlaces relativos en otros documentos. Las citas por ID en comentarios pueden seguir. El mensaje de [tools/mutar.py](../../../tools/mutar.py) debe dar una URL accesible al instalado desde PyPI. |
| **Dos tutoriales** | Mover `ESCRIBIR-UNA-MEDIDA.md` y `ORACLE-TUTORIAL-PRACTICO.md` a `docs/`: enseñan a usar el lenguaje. | Conservarlos en raíz, donde funcionan hoy, a costa de dispersar el camino de lectura. | Actualizar README y `docs/README.md`; cambiar ambas rutas en `DOCUMENTOS_CON_SUPERFICIE` de [tools/sintaxis.py](../../../tools/sintaxis.py) y la lectura de `como_escribir()` en `tools/estudio.py`. Si no, falla la verificación de documentos. |
| **`editores/`** | Dejarlo **en el repo y en su ubicación actual**. Contiene código distribuible: cliente VS Code, cliente Emacs y empaquetador `.vsix`; [editores/README.md](../../../editores/README.md) enlaza su instalación con `oracle-lsp`. `docs/` debe enlazarlo, no alojar el código. | Repositorios separados por editor, útil si cada cliente adquiere publicación y mantenimiento propios; hoy añade coordinación. | Mantener el enlace de `docs/README.md` y las rutas del instructivo de empaquetado. No añadir los clientes al wheel Python: se distribuyen por otro mecanismo. |
| **`vault-kb/`** | Crear índice y `planes/` para los **18** `PLAN-*`; `estudios/` para investigaciones y sus scripts/datos de reproducción; `postmortems/` para informes como el naval con su carpeta `naval-pm/`. Los tres `RELEVO*` de raíz son el mismo puntero de una línea al tracker: **retirarlos de Git**, conservando su historia, en vez de crear `relevos/` para duplicados. El agente retoma desde `AGENTS.md` y `tareas/`. `ORACLE-PARA-NOTEBOOKLM.md` es una salida generada de 16 399 líneas: retirarla del árbol versionado y regenerarla a demanda. | Conservar los relevos en `vault-kb/relevos/` y una copia fechada del compendio en `vault-kb/compendios/`, si se necesita preservar esas instantáneas navegables. | Buscar y corregir enlaces a **los 18** planes, incluidos los 12 ausentes de la tabla; `tools/estudio.py` exige `PLAN-CORRECCION.md`. Ajustar los enlaces históricos a relevos que se retiren y el ejemplo `--archivo` del generador. **`observaciones/` permanece donde está**: corpus, tests y herramientas citan sus evidencias. `build/`, `dist/`, `estudio/` y `*.egg-info/` ya están ignorados y no versionados; no requieren `git rm`. |

Además, movería `estudios/MCP-CONTRATO.md` a `docs/mcp-contrato.md`: es contrato del producto. Su bloque JSON **ya se genera** desde `HERRAMIENTAS`; hay que cambiar las rutas de [tools/mcp_contrato.py](../../../tools/mcp_contrato.py), [tests/test_mcp.py](../../../tests/test_mcp.py) y README. La [web](../../../docs/index.html) enlaza las decisiones mediante el ancla del README, no mediante rutas directas; conviene corregir también su texto que aún dice «ocho». No modifiqué archivos.